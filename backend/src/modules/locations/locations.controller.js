const prisma = require("../../config/database");
const { ok, fail } = require("../../utils/api-response");
const { logActivity } = require("../../utils/logger");
function codeify(value) { return String(value || "LOC").trim().toUpperCase().replace(/[^A-Z0-9]+/g, "-").replace(/^-|-$/g, "").slice(0, 60) || `LOC-${Date.now()}`; }
function data(body) { const name = body.name || body.nameEn || body.name_en || body.nameTr || body.name_tr; return { name, nameTr: body.nameTr || body.name_tr || name || null, nameEn: body.nameEn || body.name_en || name || null, code: body.code || codeify(name), description: body.description || body.descriptionEn || body.descriptionTr || null, descriptionTr: body.descriptionTr || body.description_tr || body.description || null, descriptionEn: body.descriptionEn || body.description_en || body.description || null, parentId: body.parentId || body.parent_id || null }; }
async function list(req, res) { return ok(res, "Locations listed", await prisma.storageLocation.findMany({ include: { parent: true }, orderBy: { name: "asc" } })); }
async function get(req, res) { const item = await prisma.storageLocation.findUnique({ where: { id: Number(req.params.id) }, include: { parent: true, children: true } }); if (!item) return fail(res, "Location not found", [], 404); return ok(res, "Location detail", item); }
async function create(req, res) { const item = await prisma.storageLocation.create({ data: data(req.body) }); await logActivity(prisma, req, "CREATE", "storage_location", item.id, null, item); return ok(res, "Location created", item, 201); }
async function update(req, res) { const old = await prisma.storageLocation.findUnique({ where: { id: Number(req.params.id) } }); if (!old) return fail(res, "Location not found", [], 404); const item = await prisma.storageLocation.update({ where: { id: old.id }, data: data(req.body) }); await logActivity(prisma, req, "UPDATE", "storage_location", item.id, old, item); return ok(res, "Location updated", item); }
async function remove(req, res) { const old = await prisma.storageLocation.findUnique({ where: { id: Number(req.params.id) } }); if (!old) return fail(res, "Location not found", [], 404); await prisma.storageLocation.delete({ where: { id: old.id } }); await logActivity(prisma, req, "DELETE", "storage_location", old.id, old, null); return ok(res, "Location deleted"); }
module.exports = { list, get, create, update, remove };